n=int(input())
for i in range(n):
  s=[*map(int,input().split())]
  x=s[1]-s[0]
  if(x!=(s[2]-s[1])):
    ans=s[-1]*(s[1]/s[0])
    ans=int(ans) if(isinstance(ans,float) and (ans.is_integer())) else ans
    print(*s,ans)
  else:
    print(*s,s[-1]+x)
